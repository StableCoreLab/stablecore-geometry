SCGeometry / Geometry 仓库 AI 协作规则
技术栈：C++20 / Qt 6 / CMake 
所有源码文件统一使用 UTF-8
禁止使用 PowerShell Default 编码读写源码
本文件是 AI 每次任务都会读取的最小规则，仅包含运行时必要约束。
详细架构与设计文档按需加载，不在此展开。

---

# 1. 工作模式（P0 强约束）

本仓库采用 AI 离线生成模式：

- AI 只负责：
  - 修改代码
  - 修改 CMake
  - 编写或更新文档

- AI 严格禁止：
  - 编译（cmake / msbuild / ninja / make）
  - 运行程序 / 测试 / 工具


- 不得修改：
  - build/
  - 任何生成目录
  - 临时目录

代码正确性必须通过：

- 静态逻辑推断
- 与现有代码一致性
- 与设计文档对照

---

# 2. 修改边界

必须：

- 只修改与当前任务直接相关的文件
- 保持现有模块划分
- 采用最小修改原则
- 保持接口兼容（除非任务明确要求）
- 不回滚用户已有改动
- 不引入新依赖（除非任务明确要求）

禁止：

- 扩散修改范围
- 重写已有系统
- 引入隐式行为（隐式写入 / 隐式状态）
- 提交临时代码或调试代码
- 修改第三方源码（除非任务明确要求）

---
# 3. 文档语言
- 新增或更新仓库内的 Markdown、设计说明、审查记录、迁移说明和面向使用者的说明文档时，统一使用中文。
- 代码符号、文件路径、命令、协议名及必要的专有技术术语保持其原始写法；其余说明文字使用中文。
- 仅在外部格式、上游规范或用户明确要求英文时使用英文，并在文档中说明原因。
